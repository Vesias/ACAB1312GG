class StratManager {
    saveStrat(strat, visibility) {
        // Pseudocode zum Speichern der Strat im Backend
        console.log(`Strat gespeichert mit Sichtbarkeit: ${visibility}`);
    }

    shareStrat(stratId: any , target: any) {
        // Pseudocode zum Teilen der Strat mit anderen Spielern oder der community
        console.log(`Strat ${stratId} geteilt mit ${target}`);
    }

    // Weitere Methoden zur Verwaltung von Strategien
}
